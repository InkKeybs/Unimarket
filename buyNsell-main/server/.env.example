# Example .env for the server
# Copy this to .env and fill in real values (do NOT commit .env with secrets)

# MongoDB connection string (Atlas). Example:
# ATLAS_KEY=mongodb+srv://<username>:<password>@cluster0.mongodb.net/<dbname>?retryWrites=true&w=majority
ATLAS_KEY=

# JWT secrets
JWTPRIVATEKEY=
JWTREFRESHPRIVATEKEY=

# Email (nodemailer) settings
SERVICE=Gmail
USER=
PASS=

# Optional SMTP host-based config (recommended for cloud deployments)
# Example (Brevo):
# SMTP_HOST=smtp-relay.brevo.com
# SMTP_PORT=587
# SMTP_SECURE=false
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false

# Optional SMTP timeouts (milliseconds)
SMTP_CONNECTION_TIMEOUT=15000
SMTP_GREETING_TIMEOUT=15000
SMTP_SOCKET_TIMEOUT=20000

# Frontend origin(s) allowed by CORS (comma-separated)
# Example: https://unimarket.vercel.app
CORS_ORIGIN=

# Base URL used in emails
# Example: https://unimarket.vercel.app/
BASE_URL=

# Server port (optional, defaults to 5000)
PORT=5000
